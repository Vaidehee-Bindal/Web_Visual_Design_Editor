import type { CanvasDocument } from '../../services/api';
type Props = { canvases: CanvasDocument[]; activeId?: string; onLoad: (id: string) => void; onDelete: (id: string) => void };
export default function CanvasList({ canvases, activeId, onLoad, onDelete }: Props) { return <section className="saved-section"><div className="saved-heading"><div><span className="eyebrow">Workspace</span><h2>Saved canvases</h2></div><span className="count-pill">{canvases.length}</span></div>{canvases.length === 0 ? <p className="muted">Your saved designs will appear here.</p> : <div className="canvas-list">{canvases.map((canvas) => <div className={`canvas-card ${activeId === canvas._id ? 'active' : ''}`} key={canvas._id} onClick={() => canvas._id && onLoad(canvas._id)}><div className="canvas-thumb"><span /></div><div className="canvas-meta"><strong>{canvas.name}</strong><small>{canvas.width} × {canvas.height}</small></div><button className="remove-canvas" onClick={(e) => { e.stopPropagation(); canvas._id && onDelete(canvas._id); }}>×</button></div>)}</div>}</section>; }
