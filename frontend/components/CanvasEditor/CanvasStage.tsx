'use client';
import { useEffect, useRef } from 'react';
import { Stage, Layer, Rect, Circle, Text, Line, Transformer } from 'react-konva';
import type Konva from 'konva';
import type { CanvasElement } from '../../services/api';
type Props = { width: number; height: number; zoom?: number; elements: CanvasElement[]; selectedId: string | null; onSelect: (id: string | null) => void; onChange: (id: string, patch: Partial<CanvasElement>) => void; stageRef: React.MutableRefObject<Konva.Stage | null> };
export default function CanvasStage({ width, height, zoom = 1, elements, selectedId, onSelect, onChange, stageRef }: Props) { const transformerRef = useRef<Konva.Transformer>(null); const nodeRefs = useRef<Record<string, Konva.Node>>({}); useEffect(() => { const node = selectedId ? nodeRefs.current[selectedId] : null; transformerRef.current?.nodes(node ? [node] : []); transformerRef.current?.getLayer()?.batchDraw(); }, [selectedId, elements]); const commitTransform = (el: CanvasElement, node: Konva.Node) => { const scaleX = node.scaleX(); const scaleY = node.scaleY(); node.scaleX(1); node.scaleY(1); if (el.type === 'circle') onChange(el.id, { x: node.x(), y: node.y(), radius: Math.max(8, (el.radius || 50) * scaleX), rotation: node.rotation() }); else if (el.type === 'line' || el.type === 'curve') onChange(el.id, { x: node.x(), y: node.y(), rotation: node.rotation(), points: (el.points || []).map((point, index) => point * (index % 2 ? scaleY : scaleX)) }); else onChange(el.id, { x: node.x(), y: node.y(), width: Math.max(20, (el.width || 100) * scaleX), height: Math.max(20, (el.height || 80) * scaleY), rotation: node.rotation() }); }; return <Stage ref={stageRef} width={width * zoom} height={height * zoom} scaleX={zoom} scaleY={zoom} onMouseDown={(e) => { if (e.target === e.target.getStage()) onSelect(null); }}><Layer>{elements.map((el) => { const common = { key: el.id, id: el.id, x: el.x, y: el.y, rotation: el.rotation, opacity: el.opacity ?? 1, draggable: true, ref: (node: Konva.Node | null) => { if (node) nodeRefs.current[el.id] = node; }, onClick: () => onSelect(el.id), onTap: () => onSelect(el.id), onDblClick: () => onSelect(el.id), onDblTap: () => onSelect(el.id), onDragEnd: (e: Konva.KonvaEventObject<DragEvent>) => onChange(el.id, { x: e.target.x(), y: e.target.y() }), onTransformEnd: (e: Konva.KonvaEventObject<Event>) => commitTransform(el, e.target) }; if (el.type === 'rectangle') return <Rect {...common} width={el.width} height={el.height} fill={el.fill} cornerRadius={12} />; if (el.type === 'circle') return <Circle {...common} radius={el.radius} fill={el.fill} />; if (el.type === 'line' || el.type === 'curve') return <Line {...common} points={el.points} bezier={el.type === 'curve'} stroke={el.stroke || el.fill} strokeWidth={el.strokeWidth || 4} lineCap="round" lineJoin="round" />; return <Text {...common} text={el.text} fontSize={el.fontSize} fontFamily={el.fontFamily || 'Arial'} fontStyle={el.fontStyle || 'normal'} textDecoration={el.textDecoration || 'none'} align={el.align || 'left'} fill={el.fill} padding={4} width={el.width} />; })}<Transformer ref={transformerRef} rotateEnabled={true} enabledAnchors={['top-left','top-right','bottom-left','bottom-right']} borderStroke="#8b5cf6" anchorStroke="#8b5cf6" anchorFill="#ffffff" anchorSize={8} /></Layer></Stage>; }
