import type { ElementType } from '../../services/api';
type Props = { onAdd: (type: ElementType) => void; onNew: () => void; onSave: () => void; onExport: () => void; onUndo: () => void; onRedo: () => void; onZoom: (zoom: number) => void; onFullscreen: () => void; zoom: number; canUndo: boolean; canRedo: boolean; saving: boolean };
export default function Toolbar({ onAdd, onNew, onSave, onExport, onUndo, onRedo, onZoom, onFullscreen, zoom, canUndo, canRedo, saving }: Props) { return <header className="toolbar"><div className="brand"><span className="brand-mark">✦</span><span>canvasly</span></div><div className="tagline">Design. Create. Save. <span>✦</span></div><div className="tool-group"><button className="tool-button" onClick={() => onAdd('rectangle')}><span className="shape-icon rectangle-icon" />Rectangle</button><button className="tool-button" onClick={() => onAdd('circle')}><span className="shape-icon circle-icon" />Circle</button><button className="tool-button" onClick={() => onAdd('text')}><span className="text-icon">T</span>Text</button><button className="tool-button" onClick={() => onAdd('line')}><span className="line-icon">╱</span>Line</button><button className="tool-button" onClick={() => onAdd('curve')}><span className="curve-icon">∿</span>Curve</button></div><div className="toolbar-actions"><button className="icon-button" disabled={!canUndo} onClick={onUndo} title="Undo">↶</button><button className="icon-button" disabled={!canRedo} onClick={onRedo} title="Redo">↷</button><select className="zoom-select" value={zoom} onChange={(e) => onZoom(Number(e.target.value))}><option value={0.6}>60%</option><option value={0.8}>80%</option><option value={1}>100%</option><option value={1.2}>120%</option></select><button className="icon-button" onClick={onFullscreen} title="Fullscreen">⛶</button><button className="subtle-button" onClick={onExport}>⇩ Export PNG</button><button className="save-button" onClick={onSave}>{saving ? 'Saving…' : 'Save canvas'} <span>↗</span></button><span className="avatar">V</span></div></header>; }
