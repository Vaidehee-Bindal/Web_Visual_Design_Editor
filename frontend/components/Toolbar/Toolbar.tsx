import type { ElementType } from '../../services/api';
type Props = { onAdd: (type: ElementType) => void; onNew: () => void; onSave: () => void; onExport: () => void; onUndo: () => void; onRedo: () => void; canUndo: boolean; canRedo: boolean; saving: boolean };
export default function Toolbar({ onAdd, onNew, onSave, onExport, onUndo, onRedo, canUndo, canRedo, saving }: Props) { return <header className="toolbar"><div className="brand"><span className="brand-mark">✦</span><span>canvasly</span></div><div className="tool-group"><button className="tool-button" onClick={() => onAdd('rectangle')}><span className="shape-icon rectangle-icon" />Rectangle</button><button className="tool-button" onClick={() => onAdd('circle')}><span className="shape-icon circle-icon" />Circle</button><button className="tool-button" onClick={() => onAdd('text')}><span className="text-icon">T</span>Text</button></div><div className="toolbar-actions"><button className="icon-button" disabled={!canUndo} onClick={onUndo} title="Undo">↶</button><button className="icon-button" disabled={!canRedo} onClick={onRedo} title="Redo">↷</button><button className="subtle-button" onClick={onNew}>New canvas</button><button className="subtle-button" onClick={onExport}>Export PNG</button><button className="save-button" onClick={onSave}>{saving ? 'Saving…' : 'Save canvas'} <span>↗</span></button></div></header>; }
